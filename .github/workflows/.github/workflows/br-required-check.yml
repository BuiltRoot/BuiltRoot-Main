name: BR Required Check
on:
  workflow_dispatch:          # gives you the "Run workflow" button
  pull_request:
  push:
    branches: [main]

jobs:
  br_required_job:
    name: br-check
    runs-on: ubuntu-latest
    steps:
      - name: Repo checkout
        uses: actions/checkout@v4
      - name: Sanity step
        run: echo "✅ BR required check ran successfully."
