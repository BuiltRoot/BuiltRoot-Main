# BuiltRoot Docs

- `../roadmaps/` — product roadmaps (living docs)
- `templates/` — copy blocks, design snippets, checklists

Authoring rules:
- Keep files small and scannable.
- Prefer Markdown.
- Link out to external sources instead of pasting long texts.
